# Results

Use this folder for derived artifacts:
- `figures/` — publication-quality plots
- `tables/` — CSV summaries and metrics

Outputs should be generated reproducibly from scripts in `scripts/` or notebooks. Do not commit heavy intermediate files.
